{
  "name": "bookmark-lite-mcp",
  "version": "1.0.1-beta.1",
  "description": "MCP server exposing Bookmark Lite (LinkFlow) bookmark & tag capabilities as standard MCP tools for AI agents.",
  "type": "module",
  "bin": {
    "bookmark-lite-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "NODE_ENV=production node build.mjs",
    "build:local": "node build.mjs",
    "watch": "node build.mjs --watch",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run typecheck && npm run build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "bookmark",
    "linkflow",
    "ai-agent"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.9.3"
  }
}
